{
  "name": "secret-hitler",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/davidx1/secret-hitler.git",
  "author": "David Xu <davidxu1991@gmail.com>",
  "license": "MIT",
  "scripts": {
    "client:dev": "cd client && yarn dev",
    "client:build": "cd client && yarn && yarn build",
    "server:dev": "cd server && yarn dev",
    "server:start": "cd server && yarn && yarn start",
    "dev": "concurrently - kill-others \"yarn run server:dev\" \"yarn run client:dev\"",
    "start": "yarn run server:start",
    "heroku-postbuild": "yarn run client:build"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
